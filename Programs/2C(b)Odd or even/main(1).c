#include <stdio.h>
void main()
{
    int A;
    printf("Enter an integer to Check its Even OR Odd:");
    scanf("%d",&A);
    if(A%2==0)
    {
        printf("Number is Even");

    }
    else
    {
        printf("Number is Odd");
    }
}
